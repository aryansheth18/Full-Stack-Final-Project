{
  "version": 2,
  "buildCommand": "cd server && npm install && npx prisma generate && npm run build && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/dist/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
